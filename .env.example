ADZUNA_APP_ID=
ADZUNA_APP_KEY=
JOOBLE_KEY=
CAREERJET_AFFID=
USAJOBS_KEY=
USAJOBS_EMAIL=

# Hosted deploy only. `npm run push:remote` mirrors the local corpus into this database and
# the app reads it; leave both unset and the app reads ./workie.db instead.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
